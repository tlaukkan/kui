package org.kui.util

import org.junit.Ignore
import org.junit.Test

class DateTimeTest {

    @Test
    @Ignore
    fun testLocalUtcConversion() {
        println(utcStartOfDay())
        //println(localToUtc(Date()))
        //println(utcToLocal(localToUtc(Date())))
    }

}